BEGIN TRY

BEGIN TRAN;

IF COL_LENGTH('dbo.Solucoes', 'Status') IS NOT NULL
BEGIN
  ALTER TABLE [dbo].[Solucoes] DROP COLUMN [Status];
END

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
